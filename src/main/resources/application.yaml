spring:
  application:
    name: bloodbridge

  datasource:
    url: jdbc:postgresql://localhost:5432/bloodbridge
    username: postgres
    password: radhe

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

